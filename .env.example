# Groq API 키 (console.groq.com에서 발급)
GROQ_API_KEY=your_groq_api_key_here

# Firebase 서비스 계정 키 (JSON을 base64로 인코딩한 문자열)
# 로컬: serviceAccountKey.json 파일 사용
# Render: 아래 변수에 base64 인코딩된 전체 JSON 넣기
FIREBASE_SERVICE_ACCOUNT_BASE64=

# 서버 포트 (Render는 자동으로 PORT 환경변수 설정)
PORT=3000
